apply plugin: "war"

bootWar {
    mainClass = "com.sothis.tech.JhipsterApiApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
